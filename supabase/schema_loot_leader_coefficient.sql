-- L2 Clan Cabinet — «Коэффициент пати-лидерам»: отдельный множитель, который
-- применяется ТОЛЬКО к личным финальным баллам того, кто назначен лидером пати
-- (clan_groups.leader_nickname) — поверх его обычного коэффициента профессии,
-- только в «ДКП Пати» (личная строка внутри развёрнутого списка участников).
-- На саму пати (агрегированную строку) и на «ДКП Соло» не влияет.
-- Настраивается в Админ-панели → «ДКП». По умолчанию 1 — до первой настройки
-- ничего не меняется для уже существующих кланов.
-- Выполнить в Supabase Dashboard → SQL Editor (после schema_loot_split_settings.sql)

alter table public.loot_split_settings
  add column if not exists loot_leader_coefficient numeric not null default 1;
