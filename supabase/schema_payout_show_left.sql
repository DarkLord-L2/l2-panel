-- L2 Clan Cabinet — «Раздача»: показывать ли в списке тех, кто уже вышел из
-- клана (настраивается в Админ-панели → «ДКП»). Их голда всё равно делится
-- поровну между оставшимися в клане в том же списке лидера — этот тумблер
-- влияет только на то, видна ли сама строка ушедшего (с его исходной, ещё не
-- поделённой суммой — для справки), не на саму логику перераспределения.
-- По умолчанию включено (true) — поведение как было до этой настройки.
-- Выполнить в Supabase Dashboard → SQL Editor (после schema_loot_split_settings.sql)

alter table public.loot_split_settings
  add column if not exists show_left_in_payout boolean not null default true;
