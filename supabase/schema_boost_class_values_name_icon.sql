-- «Проверка буста»: «Значения по классам» теперь может задавать не только
-- текст-подпись, но и своё НАЗВАНИЕ и КАРТИНКУ пункта для конкретного класса
-- (например, у мага «Кольцо Архимага» со своей иконкой, у файтера — «Кольцо
-- Дуэлянта» с другой). Работает независимо от boost_items.per_class (та
-- галочка по-прежнему только про текст-подпись) — если для класса участника
-- задано своё name/icon, оно просто подставляется вместо общего name/icon пункта.
alter table public.boost_class_values add column if not exists name text;
alter table public.boost_class_values add column if not exists icon text;
